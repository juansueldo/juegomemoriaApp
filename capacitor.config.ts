import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.juegomemoriaapp',
  appName: 'juegomemoriaApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
